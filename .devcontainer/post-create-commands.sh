#!/usr/bin/bash

git config --global user.name "Arno Gobbin"
git config --global user.email "32413451+a-gn@users.noreply.github.com"
git config --global user.signingkey "DE8F13577EB74FA7"
# Can't get gpg-agent to be forwarded to the container in VSCode
# git config --global commit.gpgsign true
